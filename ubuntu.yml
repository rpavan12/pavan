---
- hosts: ubuntunode
  become: yes
  tasks:
  - name: i want to install apache2
    apt:
      name: apache2
      state: present
  - name: start the service
    service:
      name: apache2
      state: started