---
- hosts: ubuntunode
  become: yes
  tasks:
  - name: i want to install apache2
    apt:
      name: apache2
      status: present
  - name: start the service
    service:
      name: apache2
      state: started